---
name: release
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/release.yml
      - package.json
permissions:
  actions: read
  contents: write
  id-token: write
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: softprops/turnstyle@3805450be63b8c80577dc253e8c17e9132036df4 # v3.3.3
        env:
          GITHUB_TOKEN: ${{ github.token }}
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Determine version and release status
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PACKAGE_VERSION="v$(yq -r .version package.json)"
          echo "PACKAGE_VERSION=$PACKAGE_VERSION" >> "$GITHUB_ENV"

          RELEASE_VIEW_OUTPUT="$(gh release view "$PACKAGE_VERSION" 2>&1)" && CHANGED=false || CHANGED=true
          {
            echo "RELEASE_VIEW_OUTPUT<<RELEASE_VIEW_OUTPUT_EOF"
            echo "$RELEASE_VIEW_OUTPUT"
            echo "RELEASE_VIEW_OUTPUT_EOF"
          } >> "$GITHUB_ENV"
          echo "CHANGED=$CHANGED" >> "$GITHUB_ENV"
      - name: Verify the release is genuinely absent
        if: env.CHANGED == 'true'
        run: |
          if ! (echo "$RELEASE_VIEW_OUTPUT" | grep -qi "release not found")
          then
            echo "::error::Failed to check whether $PACKAGE_VERSION already has a release: $RELEASE_VIEW_OUTPUT"
            exit 1
          fi
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        if: env.CHANGED == 'true'
        with:
          bun-version-file: .bun-version
          no-cache: true
      - uses: flatt-security/setup-takumi-guard-npm@6d4182745c1e474c35a023573c2612c085be45a4 # v1.2.0
        if: env.CHANGED == 'true'
      - run: bun install
        if: env.CHANGED == 'true'
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        if: env.CHANGED == 'true'
        with:
          node-version: lts/*
          package-manager-cache: false
      - run: npm publish --provenance --registry https://registry.npmjs.org/
        if: env.CHANGED == 'true'
      - name: Create release
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        if: env.CHANGED == 'true'
        with:
          script: |
            const {createRequire} = require('node:module')
            const {pathToFileURL} = require('node:url')
            const {tsImport} = createRequire(
              `${process.env.GITHUB_WORKSPACE}/package.json`
            )('tsx/esm/api')
            const {script} = await tsImport(
              './scripts/release/release/create_release.ts',
              pathToFileURL(`${process.env.GITHUB_WORKSPACE}/`).href
            )
            await script(github, context)
